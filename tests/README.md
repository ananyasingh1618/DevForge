# Cross-cutting integration tests

Integration tests that exercise the running API end-to-end (not unit tests, which live inside
`api/` and `frontend/`). The register → login → create project → list project test is added
here in Milestone 11.
