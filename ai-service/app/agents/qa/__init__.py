"""Codebase Q&A: answers a question grounded in Phase 8 retrieval results,
via Anthropic Claude structured output. The model never outputs a file
path, symbol name, or line number itself — only which of the numbered
sources it drew from; see docs/QA_PHASE_PLAN.md for why."""
