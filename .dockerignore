node_modules
**/node_modules
.git
**/dist

# Secrets and local-tool state must never enter a build context (and so
# never an image layer). Broader than `.env` alone: `.env.local` and
# `.env.production` hold real values, `.vercel/` holds a CLI token.
**/.env
**/.env.*
**/.vercel
.run
backups
.claude

ai-service/.venv
ai-service/**/__pycache__
