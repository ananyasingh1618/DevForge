/**
 * Bump this whenever a case is added, removed, or its ground truth changes
 * in a way that would make a prior evaluation report not comparable to a
 * new one. Every generated report embeds this value (see
 * src/report.ts) so a report is always self-describing about which
 * version of the dataset produced it.
 */
export const DATASET_VERSION = "2026.09.16-2";
